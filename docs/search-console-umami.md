# Search Console + Umami (SEO loop)

## 1) Verify the site in Google Search Console

Use a **Domain property** (preferred over URL prefix). It covers apex (`useorigin.app`), `www`, and any future subdomain in one verification.

1. Open https://search.google.com/search-console
2. **Add property** → **Domain** → enter `useorigin.app`
3. Google shows a `google-site-verification=...` TXT record
4. Add it as a TXT record on the apex in your DNS provider (we use **GoDaddy**):
   - GoDaddy → Domains → `useorigin.app` → **DNS** → **Add New Record**
   - Type: **TXT**, Name: **@**, Value: the full `google-site-verification=...` string, TTL: 1 hour
5. Wait ~5 minutes, confirm propagation: `dig TXT useorigin.app +short`
6. Click **Verify** in Search Console

Note: if `useorigin.app` was previously verified for Google Workspace (or any other Google product) under the same Google account, GSC will auto-verify against that record — no DNS change needed.

## 2) Submit the sitemap

In Search Console → **Sitemaps**, submit: `sitemap.xml`

The sitemap is auto-generated by Next.js from `src/app/sitemap.ts`.

## 3) How this "integrates" with Umami (the useful workflow)

There is no native product-level linking between Search Console and Umami. The practical integration is a weekly workflow:

- **Search Console tells you what to rank for**
  - Queries with high impressions + positions **8–30** (striking distance)
  - Pages getting impressions but low CTR (title/meta tuning)

- **Umami tells you what happens after the click**
  - Landing page engagement and drop-off
  - CTA events (Get started, View on GitHub, Waitlist submit) — once event tracking is added

## 4) Suggested weekly routine (15 minutes)

1. Search Console → Performance → **Queries**
   - Filter position 8–30
   - Pick top 3 by impressions
2. Click each query → see the **Pages** tab
3. Open those pages in Umami → confirm they are getting traffic
4. Update the page:
   - tighten title/H1 for query intent
   - add 1–2 missing sections
   - add 2–4 internal links to/from related guides
