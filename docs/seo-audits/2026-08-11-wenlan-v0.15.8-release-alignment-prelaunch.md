# Wenlan v0.15.8 release alignment — local prelaunch audit

Captured at: `2026-08-12T06:08:09Z`

Status: locally prepared and verified; not committed, pushed, merged, or deployed.

## Why this correction is needed

GitHub reports `v0.15.8` as the latest public non-prerelease Wenlan release,
published at `2026-08-09T17:34:28Z`. Production still presents `v0.15.3` on
its release and download surfaces. This is a public-product accuracy defect,
not a search-demand experiment, and no traffic or ranking lift is assigned to
the correction.

The release contains four native runtime archives and a macOS Apple Silicon
desktop DMG. The desktop app now lives in the main Wenlan repository under
`app/`, is version-locked to the workspace release, and uses the unified Wenlan
updater manifest. The app crate remains `AGPL-3.0-only`; the runtime and plugin
surface remains `Apache-2.0`. The macOS desktop preview is not notarized, so
the public copy links to the maintained, inspectable installer instructions
instead of weakening the system-wide security boundary.

## Prepared public changes

- Update the public release contract from `v0.15.3` to `v0.15.8`.
- Add the `Wenlan_0.15.8_aarch64.dmg` desktop preview as the recommended
  macOS download and keep `wenlan-darwin-arm64.tar.gz` as a separate headless
  runtime row.
- Preserve the Windows runtime ZIP boundary; do not describe it as a Windows
  desktop app.
- Update English, zh-TW, and zh-CN home, download, About, and Get Started
  surfaces without changing acquisition routing.
- Update release, desktop-app, product-matrix, security, structured-data,
  sitemap-date, `llms.txt`, and `llms-full.txt` facts.
- Replace current public links to the historical split `wenlan-app` repository
  with the versioned main-repository `app/` source.
- Extend deterministic tests so a future release cannot silently drift on the
  DMG/runtime distinction, app source, updater manifest, license boundary, or
  translated safety note.

## Source-native release evidence

GitHub REST returned 47 total stars and latest release `v0.15.8`. The exact
website-linked assets were checked read-only:

| Asset | Public response | Public size |
| --- | ---: | ---: |
| `wenlan-windows-x64.zip` | `302` download redirect | 73.3 MiB |
| `Wenlan_0.15.8_aarch64.dmg` | `302` download redirect | 82.3 MiB |
| `wenlan-darwin-arm64.tar.gz` | `302` download redirect | 49.8 MiB |
| `wenlan-linux-x64.tar.gz` | `302` download redirect | 63.9 MiB |
| `wenlan-linux-arm64.tar.gz` | `302` download redirect | 62.7 MiB |

The tagged desktop-app and runtime-install instructions each returned `200`.
No download was performed and no download counter is inferred from these
responses.

## Deterministic verification

- `pnpm seo:goal:check`: pass before preparation.
- `pnpm test:seo` with explicit Wenlan and wenlan-app checkout roots: 222/222
  pass.
- `pnpm test:i18n`: 63/63 pass.
- `pnpm lint`: pass.
- `pnpm build`: pass; 223 static/generated pages; IndexNow correctly skipped
  because `VERCEL_ENV` was unset.
- `pnpm seo:technical:built`: pass; 26 redirects, seven noindex headers, 120
  sitemap URLs, 24 required HTML pages, and no `FAQPage` across 124 built HTML
  pages.
- `git diff --check`: pass.

## Rendered QA

Fresh local-production captures covered all 16 changed routes at both
`393x852` and `1440x1000`: the three homepages, three download pages, three
About pages, three Get Started pages, Product Matrix, Security, Desktop App,
and Changelog. Top, middle, and bottom frames were opened for every route.
All document widths remained below the requested viewport; no page overflowed.

Pass A — design-system and functional integrity: **PASS**. The release data is
centralized in `src/lib/releases.ts`, optional platform labels are typed in the
i18n schema, and the existing `DownloadPlatforms` component renders all rows.
No raster image or ad-hoc duplicate page stands in for the interface. Existing
links, expandable install rows, and localized route structure remain intact.

Pass B — visual and CJK precision: **PASS**. Exact-size live-production
screenshots were used as the structural baseline. Reduced-motion homepage
captures score 100/100 similarity in all six locale/viewport pairs. Intended
copy changes account for the remaining differences: download pages score
88–96 mobile and 95–96 desktop; About 98–100 mobile and 99 desktop; Get
Started 98–99 mobile and 98 desktop; the English supporting docs score 88–99
mobile and 97–99 desktop. All alpha channels are intact. Direct inspection of
each reported hotspot maps it to the new v0.15.8 text or added desktop/runtime
row; no unexplained layout movement, clipping, tofu, one-character CJK orphan,
or detached phrase remains.

Evidence directory: `/private/tmp/wenlan-v0158-qa`.

## Exact publication manifest

The current campaign worktree is not a safe publication branch. A read-only
fetch on `2026-08-12` confirmed that `origin/main` is now
`9d49f9c4fdb1e2ba64f946cc7e4e0fe1236bb84e`, which already contains the
English source-backed AI knowledge-base snippet and the three-language
tool-selection homepage links. Publication must therefore start from that
exact `origin/main` commit and apply only the following release-correction
files:

- `public/llms.txt`
- `scripts/app-contract.test.mjs`
- `scripts/brand-contract.test.mjs`, limited to the v0.15.8 release and
  download assertions; do not carry the campaign-artifact scan exclusion
- `scripts/seo-github-fetch.test.mjs`
- `src/app/(en)/about/opengraph-image.tsx`
- `src/app/(en)/docs/docs.ts`
- `src/app/(en)/llms-full.txt/route.ts`
- `src/app/sitemap.ts`
- `src/app/structured-data.ts`
- `src/components/download/download-platforms.tsx`
- `src/i18n/content/en.ts`
- `src/i18n/content/schema.ts`
- `src/i18n/content/zh-CN.ts`
- `src/i18n/content/zh-TW.ts`
- `src/lib/releases.ts`
- this audit record

The publication branch must explicitly exclude `PLAN.md`, `EXPERIMENTS.md`,
all earlier campaign audits, `scripts/seo-weekly.test.mjs`,
`src/app/_pages/home.tsx`, and `src/app/(en)/learn/seo-articles.ts`. Those
files are either campaign-only records or changes already present on current
`origin/main`; including their stale worktree forms would delete or duplicate
published acquisition work.

The exact scope was rebuilt without a commit in the isolated worktree
`/tmp/wenlan-v0158-candidate` from the stated `origin/main` commit. The clean
candidate preserves the already-published source-backed snippet and localized
tool-selection homepage links, and contains exactly 15 modified release files.
It also corrected four additional public statements that still described the
desktop app as a separate repository; the release contract now rejects those
stale statements.

Fresh clean-candidate verification passed: `pnpm test:seo` 222/222,
`pnpm test:i18n` 63/63, `pnpm lint`, `pnpm build` with 223 generated pages,
`pnpm seo:technical:built`, and `git diff --check`. The built audit confirms
26 redirects, seven noindex headers, 120 sitemap URLs, 24 required HTML pages,
and no `FAQPage` across 124 built HTML pages. The candidate remains detached
and uncommitted; no push, PR, merge, or deployment occurred.

## Publication boundary

This record does not authorize a commit, push, pull request, merge, Vercel
deployment, IndexNow force, request indexing, GSC validation, external post,
directory submission, paid action, synthetic analytics event, or metric
definition change. Publication requires the user's explicit approval of this
exact correction scope.
